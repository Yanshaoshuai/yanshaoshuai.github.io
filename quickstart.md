# 快速开始

欢迎！这里是快速上手指南。

## 环境准备

确保你已安装：

- 现代浏览器（Chrome、Firefox、Safari、Edge）
- Git（可选，用于本地预览）

## 本地预览

```bash
# 克隆仓库
git clone https://github.com/yourname/yourname.github.io.git
cd yourname.github.io

# 启动本地服务器
python3 -m http.server 3000

# 访问 http://localhost:3000
```

或使用 Node.js：

```bash
npx serve .
```

## 编辑文档

所有文档都是 Markdown 格式，直接编辑 `.md` 文件即可。
